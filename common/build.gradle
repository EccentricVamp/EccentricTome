plugins {
    id 'java'
    id 'org.spongepowered.gradle.vanilla' version "${sponge_version}"
}

minecraft.version minecraft_version

dependencies {
    implementation "com.google.inject:guice:${guice_version}"
}